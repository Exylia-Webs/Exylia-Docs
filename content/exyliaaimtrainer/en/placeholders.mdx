---
title: "Placeholders"
description: "Every placeholder the plugin answers, written in full, with what it reads inside and outside a run."
---

Every placeholder below is written in full, and clicking one copies it.

## How the name is written

Everywhere outside the plugin — PlaceholderAPI, and anything reading through it — a placeholder is the
plugin name, the group `aim`, and the placeholder's own name:

```text
%exyliaaimtrainer_aim_wins%
```

Inside the plugin's own files — `scoreboards.yml`, `menus/`, `messages.yml` — the same placeholder is
written with the group and nothing in front of it, because the plugin resolves its own text without
going through PlaceholderAPI:

```text
%aim_wins%
```

<Callout type="danger" title="The word aim is part of the name">
`%exyliaaimtrainer_wins%` does **not** resolve. PlaceholderAPI hands the plugin `wins`, the plugin
registered `aim_wins`, and nothing matches. Every PlaceholderAPI form of these placeholders reads
`%exyliaaimtrainer_aim_<name>%`.
</Callout>

Every placeholder resolves for the player looking at it. Most answer something whatever the moment: `0`,
or `—` (the `menu.none` text in `messages.yml`) where a number or a name does not apply. The exceptions
are noted in their rows.

### A run, a session, a duel

Three words matter in the tables below.

- A **run** is one drill being played, from the first live target to the end. It does not exist during
  the countdown, while resting after a drill, or between two rounds of a duel. In a duel, each round is
  a run.
- A **session** is the whole solo stay in the arena: countdown, run, rest and every replay.
- A **duel** is the whole series, from the teleport to the result.

## Lifetime

Read from the player's profile. Until the profile has loaded after joining, each answers `0`, or `—` for
the times.

| Placeholder | Description |
|---|---|
| `%exyliaaimtrainer_aim_total_rating%` | The overall rating: the sum of the player's best rating in every drill. |
| `%exyliaaimtrainer_aim_sessions%` | Solo runs stored. |
| `%exyliaaimtrainer_aim_total_hits%` | Lifetime hits, solo runs and duels. |
| `%exyliaaimtrainer_aim_total_shots%` | Lifetime shots, solo runs and duels: hits plus misses. |
| `%exyliaaimtrainer_aim_best_flick%` | The fastest time between two hits ever: `183ms`. `—` when none. |
| `%exyliaaimtrainer_aim_best_reaction%` | The fastest reaction ever: `201ms`. `—` when none. |

## Duel record

| Placeholder | Description |
|---|---|
| `%exyliaaimtrainer_aim_wins%` | Duels won. |
| `%exyliaaimtrainer_aim_losses%` | Duels lost. |
| `%exyliaaimtrainer_aim_winrate%` | Wins over duels played, with its sign: `50%`. Decimals follow ExyliaLib's number format. |
| `%exyliaaimtrainer_aim_win_streak%` | Duels won in a row right now. |
| `%exyliaaimtrainer_aim_best_win_streak%` | Best run of duels won in a row. |

## Live or lifetime

These four mean the running run while there is one and the lifetime profile otherwise, so a sidebar line
and a lobby hologram can share a name. With no run and no profile loaded they leave the text unchanged.

| Placeholder | Description |
|---|---|
| `%exyliaaimtrainer_aim_hits%` | Hits of the running run; lifetime hits outside one. |
| `%exyliaaimtrainer_aim_misses%` | Misses of the running run; lifetime shots minus lifetime hits outside one. |
| `%exyliaaimtrainer_aim_shots%` | Hits plus misses of the running run; lifetime shots outside one. |
| `%exyliaaimtrainer_aim_accuracy%` | Accuracy of the running run, or lifetime accuracy, one decimal and **no** `%` sign: `87.5`. |

## The running run

Each of these answers `0`, `0.0` or `—` when there is no run.

| Placeholder | Description |
|---|---|
| `%exyliaaimtrainer_aim_score%` | The live score. |
| `%exyliaaimtrainer_aim_rating%` | The live score times the difficulty. |
| `%exyliaaimtrainer_aim_streak%` | Hits in a row. A miss, an expired target or a false start ends it. |
| `%exyliaaimtrainer_aim_best_streak%` | The longest streak of this run. |
| `%exyliaaimtrainer_aim_combo%` | The current combo, in a COMBO drill. |
| `%exyliaaimtrainer_aim_best_combo%` | The longest combo of this run. |
| `%exyliaaimtrainer_aim_time_left%` | Seconds left, rounded up: `30`, `1:30`. For a REACTION drill, what is left of its cap. |
| `%exyliaaimtrainer_aim_targets_left%` | Lights still to answer in a REACTION drill; `0` in any other. |
| `%exyliaaimtrainer_aim_grade%` | The grade of the last graded hit: `Perfect`, `Excellent`, `Good`, `Ok` or `Slow`. `—` before the first. |
| `%exyliaaimtrainer_aim_avg_flick%` | The average time between two hits: `312ms`. `—` before the second hit. |
| `%exyliaaimtrainer_aim_avg_reaction%` | The average reaction: `245ms`. `—` before the first. |
| `%exyliaaimtrainer_aim_on_target%` | Percent of the run spent on target, in a TRACK drill, one decimal and no sign: `64.2`. |
| `%exyliaaimtrainer_aim_precision%` | How central the hits were, as a percent, one decimal and no sign. |
| `%exyliaaimtrainer_aim_ping%` | The ping the run last measured. Outside a run, the player's current ping. |

## What the player is doing

| Placeholder | Description |
|---|---|
| `%exyliaaimtrainer_aim_state%` | `none`, `training` or `match`. |
| `%exyliaaimtrainer_aim_state_label%` | The same in the words of `menu.state-none`, `state-training` and `state-match`: `None`, `Drill`, `Duel`. |
| `%exyliaaimtrainer_aim_phase%` | In a solo session, `countdown`, `running` or `resting`. `none` outside one, and during a duel. |
| `%exyliaaimtrainer_aim_drill%` | The display name of the drill of the session or duel. `—` outside both. |
| `%exyliaaimtrainer_aim_drill_id%` | The drill's id from `config.yml`: `gridshot`. Empty outside a session or duel. |
| `%exyliaaimtrainer_aim_kind%` | The drill's kind in the words of `menu.kinds`: `Flicking`, `Reaction`, `Tracking`, `Combo`. `—` outside. |
| `%exyliaaimtrainer_aim_difficulty%` | The difficulty multiplier of the session or duel, two decimals: `1.35`. `1.00` outside. |

## The running duel

| Placeholder | Description |
|---|---|
| `%exyliaaimtrainer_aim_format%` | The series length: `BO3`. `—` outside a duel. |
| `%exyliaaimtrainer_aim_opponent%` | The opponent's name. `—` outside a duel. |
| `%exyliaaimtrainer_aim_opponent_score%` | The opponent's live score this round; `0` between rounds and outside a duel. |
| `%exyliaaimtrainer_aim_opponent_hits%` | The opponent's hits this round; `0` between rounds and outside a duel. |
| `%exyliaaimtrainer_aim_round%` | The round being played. `0` outside a duel. |
| `%exyliaaimtrainer_aim_series%` | Rounds won, yours first: `2 - 1`. `0 - 0` outside a duel. |
| `%exyliaaimtrainer_aim_match_state%` | `waiting`, `starting`, `active`, `round_end`, `ending` or `finished`. `none` outside a duel. |

## Leaderboards

A board is about one drill and sorted by one category, and both go in the name. Every board placeholder
comes in two forms: the plain one answers the name of the player at position `n`, and the one ending in
`_value` the number that put them there.

`<drill>` is a drill id from `config.yml` — in the file the plugin ships: `gridshot`, `scatter`,
`sixshot`, `microshot`, `spidershot`, `flick`, `precision`, `decay`, `switch`, `strafe`, `reaction`,
`tracking`, `evasive`, `combo`, `combo_19` and `axe` — and `<n>` is the position, counting from 1.

| Placeholder | Sorted by | What it answers |
|---|---|---|
| `%exyliaaimtrainer_aim_top_<drill>_rating_<n>%` | Best rating, highest first | The name of the player at that position. |
| `%exyliaaimtrainer_aim_top_<drill>_rating_<n>_value%` | The same board | Their rating: `1284`. |
| `%exyliaaimtrainer_aim_top_<drill>_score_<n>%` | Best score, highest first | The name of the player at that position. |
| `%exyliaaimtrainer_aim_top_<drill>_score_<n>_value%` | The same board | Their score, without a unit: `950`. |
| `%exyliaaimtrainer_aim_top_<drill>_accuracy_<n>%` | Best accuracy over runs with enough shots, highest first | The name of the player at that position. |
| `%exyliaaimtrainer_aim_top_<drill>_accuracy_<n>_value%` | The same board | Their accuracy, with its sign: `92.4%`. |
| `%exyliaaimtrainer_aim_top_<drill>_streak_<n>%` | Longest streak, highest first | The name of the player at that position. |
| `%exyliaaimtrainer_aim_top_<drill>_streak_<n>_value%` | The same board | Their streak: `41`. |
| `%exyliaaimtrainer_aim_top_<drill>_hits_<n>%` | Most hits in one run, highest first | The name of the player at that position. |
| `%exyliaaimtrainer_aim_top_<drill>_hits_<n>_value%` | The same board | Their hits: `96`. |
| `%exyliaaimtrainer_aim_top_<drill>_reaction_<n>%` | Fastest single reaction, **lowest** first | The name of the player at that position. |
| `%exyliaaimtrainer_aim_top_<drill>_reaction_<n>_value%` | The same board | Their reaction: `178ms`. |
| `%exyliaaimtrainer_aim_top_<drill>_combo_<n>%` | Longest combo, highest first | The name of the player at that position. |
| `%exyliaaimtrainer_aim_top_<drill>_combo_<n>_value%` | The same board | Their combo: `12`. |

The overall board goes in place of a drill as `overall`, and sorts by two categories only:

| Placeholder | Sorted by | What it answers |
|---|---|---|
| `%exyliaaimtrainer_aim_top_overall_rating_<n>%` | Overall rating, highest first | The name of the player at that position. |
| `%exyliaaimtrainer_aim_top_overall_rating_<n>_value%` | The same board | Their overall rating: `9120`. |
| `%exyliaaimtrainer_aim_top_overall_accuracy_<n>%` | Lifetime accuracy, highest first | The name of the player at that position. |
| `%exyliaaimtrainer_aim_top_overall_accuracy_<n>_value%` | The same board | Their accuracy, with its sign: `81.3%`. |

Any other category written after `overall` reads the rating board.

Written out, with a drill and a position in place:

```text
%exyliaaimtrainer_aim_top_gridshot_rating_1%
%exyliaaimtrainer_aim_top_gridshot_rating_1_value%
%exyliaaimtrainer_aim_top_reaction_reaction_3_value%
%exyliaaimtrainer_aim_top_combo_19_combo_1%
%exyliaaimtrainer_aim_top_overall_rating_1_value%
```

A drill id with underscores in it is matched whole, longest id first:
`%exyliaaimtrainer_aim_top_combo_19_combo_1%` is the `combo` board of `combo_19`, not something about
`combo`. Until a board has been read once, both forms answer the `menu.loading` text, *Loading…*, and
kick the read off; after that they keep showing the last board read while a newer one loads. A position
past the end of the board, an unknown drill, an unknown category or a position that is not a number
leaves the text unchanged. See [Statistics](/docs/exyliaaimtrainer/stats) for how a board is built and
how long it is cached.
