---
title: "Placeholders"
description: "Every placeholder the plugin answers, written in full."
---

Every placeholder below is written in full, and clicking one copies it.

## How the name is written

A placeholder is the plugin name followed by the placeholder's own name, written the same way
everywhere: through PlaceholderAPI, and inside the plugin's own files — `scoreboards.yml`, `menus/`,
`messages.yml`. That is the only identifier this plugin answers to.

```text
%exyliatotemtrainer_wins%
```

<Callout type="warning" title="Renamed after 1.0.0">
Up to `v1.0.0` every name carried the group `totem`: `%exyliatotemtrainer_totem_wins%` through
PlaceholderAPI and `%totem_wins%` inside the plugin's files. Neither form resolves any more. Write
`%exyliatotemtrainer_<name>%`, and update a `scoreboards.yml` or `menus/main.yml` already generated on
the server.
</Callout>

Every placeholder resolves for the player looking at it. One that has no value — no viewer, no profile
loaded yet, no running session — leaves the text unchanged rather than writing an empty string.

## Duel record

| Placeholder | Description |
|---|---|
| `%exyliatotemtrainer_wins%` | Duels won. |
| `%exyliatotemtrainer_losses%` | Duels lost. |
| `%exyliatotemtrainer_winrate%` | Wins over duels played, as a percentage: `50%`. |
| `%exyliatotemtrainer_best_streak%` | Best run of duel wins in a row. |

## Pops

| Placeholder | Description |
|---|---|
| `%exyliatotemtrainer_total_pops%` | Lifetime pops, training and duels together. |
| `%exyliatotemtrainer_pops%` | Pops of the running session; lifetime pops outside one. |
| `%exyliatotemtrainer_streak%` | Pops in a row of the running session; the current duel win streak outside one. |
| `%exyliatotemtrainer_best%` | Fastest reaction of the running session, or the lifetime best: `0.72s`. `—` when none. |
| `%exyliatotemtrainer_average%` | Average reaction of the running session, or the lifetime average: `0.72s`. `—` when none. |

These four are the ones a sidebar line and a lobby lore can share: the same name means the live session
while one is running and the lifetime profile otherwise.

## The running session

Training and duels both count as a session here.

| Placeholder | Description |
|---|---|
| `%exyliatotemtrainer_state%` | `none`, `training` or `match`. |
| `%exyliatotemtrainer_state_label%` | The same, in the words of `menu.state-none`, `state-training` and `state-match`: `None`, `Training`, `Duel`. |
| `%exyliatotemtrainer_mode%` | The display name of the mode being played. |
| `%exyliatotemtrainer_ticks%` | The interval the player picked, in ticks. |
| `%exyliatotemtrainer_interval%` | The interval the session is running at right now, which an accelerating mode keeps moving. Falls back to `ticks`. |
| `%exyliatotemtrainer_grade%` | The grade of the last pop: `Perfect`, `Excellent`, `Good`, `Ok` or `Slow`. `—` before the first pop; nothing outside a session. |
| `%exyliatotemtrainer_session_time%` | How long the session has been running. |

## The running duel

| Placeholder | Description |
|---|---|
| `%exyliatotemtrainer_format%` | The series length: `BO3`. |
| `%exyliatotemtrainer_opponent%` | The opponent's name. |
| `%exyliatotemtrainer_opponent_pops%` | The opponent's pops in the current round. |
| `%exyliatotemtrainer_round%` | The round being played. A replayed draw keeps its number. |
| `%exyliatotemtrainer_score%` | Rounds won, yours first: `2 - 1`. |
| `%exyliatotemtrainer_match_state%` | `waiting`, `starting`, `active`, `round_end`, `ending` or `finished`. |

## Leaderboards

A board is about one mode and sorted by one category, and both go in the name. Every board placeholder
comes in two forms: the plain one answers the player standing at position `n`, and the one ending in
`_value` the number that put them there.

`<mode>` is a mode id from `config.yml` — `normal`, `infinite`, `random`, `chaos`, `acceleration` and
`blackout` in the file the plugin ships — and `<n>` is the position, counting from 1.

| Placeholder | Sorted by | What it answers |
|---|---|---|
| `%exyliatotemtrainer_top_<mode>_rating_<n>%` | Best session score weighted by the speed it was set at, highest first | The name of the player at that position. |
| `%exyliatotemtrainer_top_<mode>_rating_<n>_value%` | The same board | Their rating: `87`. |
| `%exyliatotemtrainer_top_<mode>_best_pops_<n>%` | Most pops in one session, highest first | The name of the player at that position. |
| `%exyliatotemtrainer_top_<mode>_best_pops_<n>_value%` | The same board | Their pops: `112`. |
| `%exyliatotemtrainer_top_<mode>_fastest_average_<n>%` | Best average reaction, lowest first | The name of the player at that position. |
| `%exyliatotemtrainer_top_<mode>_fastest_average_<n>_value%` | The same board | Their average: `0.41s`. |
| `%exyliatotemtrainer_top_<mode>_best_streak_<n>%` | Longest run of pops without a fail, highest first | The name of the player at that position. |
| `%exyliatotemtrainer_top_<mode>_best_streak_<n>_value%` | The same board | Their streak: `64`. |
| `%exyliatotemtrainer_top_<mode>_longest_run_<n>%` | Longest session, highest first | The name of the player at that position. |
| `%exyliatotemtrainer_top_<mode>_longest_run_<n>_value%` | The same board | How long it lasted: `95.30s`. |

Written out, with a mode and a position in place:

```text
%exyliatotemtrainer_top_chaos_rating_1%
%exyliatotemtrainer_top_chaos_rating_1_value%
%exyliatotemtrainer_top_acceleration_fastest_average_3%
%exyliatotemtrainer_top_acceleration_fastest_average_3_value%
```

A mode id or a category with underscores in it is matched whole:
`%exyliatotemtrainer_top_my_mode_best_pops_1%` finds the mode `my_mode`. Until the board has been
read once, both forms answer the `menu.loading` text, `Loading…`, and kick the read off. A position past
the end of the board, an unknown mode or an unknown category leaves the text unchanged. See
[Stats](/docs/exyliatotemtrainer/stats) for how a board is built.
